---
import { Image } from 'astro:assets';
import logoSrc from '@images/fixsoft-logo.png';

const { class: className, ...rest } = Astro.props;
---
<Image src={logoSrc} alt="FixSoft" class={className} {...rest} />
